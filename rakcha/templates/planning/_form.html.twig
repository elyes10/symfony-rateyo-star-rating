{% extends 'base.html.twig' %}

{% block body %}

<div class="row">
        <div  style="display: flex;align-items: center;justify-content: center;margin-top: 25px; padding: 20px;">
        <div class="card">
            <div class="card-body">
            <h1 class="card-title">veuillez remplir ce formulaire</h1>
            
                {{ form_start(form,{attr:{novalidate:'novalidate'}}) }} 
                <div class="row">
                    <div class="row mb-3">
                        
                        <label for="inputText" class="col-sm-4 col-form-label">id cinema</label>
                        
                        <div class="col-sm-10">
                        {{ form_widget(form.idCin,{'attr':{'class':'form-control'}})}} 
                        {{ form_errors(form.idCin,{'attr':{'class':'text-danger'}}) }}
                        </div>
                    </div>
                </div>
                <div class="row">
                <div class="row mb-3">
               <label for="inputText" class="col-sm-4 col-form-label">date</label>
                <div class="col-sm-10">
                {{ form_widget(form.date,{'attr':{'class':'form-control'}})}}
                {{ form_errors(form.date,{'attr':{'class':'text-danger'}}) }}
                </div>
                </div>
                </div>
                <div class="row">
                <div class="row mb-3">
                <label for="inputText" class="col-sm-4 col-form-label">nom du Film</label>
                <div class="col-sm-10">
                {{ form_widget(form.nomF,{'attr':{'class':'form-control'}})}} 
                {{ form_errors(form.nomF,{'attr':{'class':'text-danger'}}) }}
                </div>
                </div>
                </div>
                <div class="row mb-3">
                <div class="col-sm-2 col-form-label">
                <div class="col-sm-10">
                <div class="btn btn-primary">
                <button class="btn">{{ button_label|default('Save') }}</button>
                </div>
                </div>
                </div>
                </div>

                {{ form_end(form) }}
                
          <a href="{{ path('app_planning_index') }}">back to list</a>
         
            </div>
            </div>
            </div>
           </div> 
    
    
{% endblock %}