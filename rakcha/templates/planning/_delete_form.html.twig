<form method="post" action="{{ path('app_planning_delete', {'idP': planning.idP}) }}" onsubmit="return confirm('vous êtes sure vous voulez le supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planning.idP) }}">
    <button class="btn">Delete</button>
</form>
