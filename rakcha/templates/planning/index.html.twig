{% extends 'base.html.twig' %}

{% block title %}Planning index{% endblock %}

{% block body %}
<div class="row">
        <div  style="display: flex;align-items: center;justify-content: center;margin-top: 30px; padding: 20px;">
   

    <table class="table table-striped table-hover table-white">
        <thead>
            <tr>
                <th>IdP</th>
                <th>IdCin</th>
                <th>Date</th>
                <th>NomF</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for planning in plannings %}
            <tr>
                <td>{{ planning.idP }}</td>
                <td>{{ planning.idCin }}</td>
                <td>{{ planning.date }}</td>
                <td>{{ planning.nomF }}</td>
                <td>
                    <a class="btn btn-outline-light" href="{{ path('app_planning_show', {'idP': planning.idP}) }}">afficher</a>
                    </td>
                    <td>
                    <a class="btn btn-info"href="{{ path('app_planning_edit', {'idP': planning.idP}) }}">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<div class="row align-items-end">
<div class="col">
<div class="position-absolute top-100 start-50 translate-middle">
    <a href="{{ path('app_planning_new') }}" class="btn btn-primary btn-lg">créer</a>
    </div>
    </div>
    </div>
    </div>
    </div>
{% endblock %}
