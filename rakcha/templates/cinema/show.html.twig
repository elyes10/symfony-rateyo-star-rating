{% extends 'base.html.twig' %}

{% block title %}Planning{% endblock %}

{% block body %}
<div class="row">
        <div  style="display: flex;align-items: center;justify-content: center;margin-top: 30px; padding: 20px;">

    <table class="table table-striped table-hover table-white">
        <tbody>
            <tr>
                <th>IdCin</th>
                <td>{{ cinema.idCin }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cinema.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ cinema.adresse }}</td>
            </tr>
            <tr>
                <th>NbreP</th>
                <td>{{ cinema.NbreP }}</td>
            </tr>
        </tbody>
    </table>
	</div>
	</div>

    <a  class="btn btn-outline-light" href="{{ path('app_cinema_index') }}">retour à la liste</a>

    <a  class="btn btn-outline-light" href="{{ path('app_cinema_edit', {'idCin': cinema.idCin}) }}">modifier</a>

    {{ include('cinema/_delete_form.html.twig') }}
{% endblock %}

